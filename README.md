# Build the images 
docker-compose build

# run the containers
docker-compose up

# access to webapp:
http://localhost:8080/

# Test flask api:
http://127.0.0.1:5000/task

# Stop containers
docker-compose stop